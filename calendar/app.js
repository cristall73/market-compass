const $=s=>document.querySelector(s),esc=v=>String(v??'—').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
function when(v){if(!v)return 'Orario da confermare';const d=new Date(v);return isNaN(d)?esc(v):new Intl.DateTimeFormat('it-IT',{timeZone:'Europe/Rome',weekday:'short',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'}).format(d)}
function specific(e,d){const txt=(e.title||'').toLowerCase(),stocks=d.investmentCandidates||[],assets=d.tradingAssets||[];let trading=assets.map(a=>a.name).slice(0,8).join(', ');let high=[];if(/inflazione|cpi|ppi|tassi|fed|bce/.test(txt))high=stocks.filter(s=>/Technology|Semiconductor|Consumer Cyclical/i.test(s.sector||'')).map(s=>`${s.name} (${s.ticker})`);else if(/pil|gdp|lavoro|employment|payroll/.test(txt))high=stocks.filter(s=>/Financial|Industrials|Consumer/i.test(s.sector||'')).map(s=>`${s.name} (${s.ticker})`);else high=stocks.map(s=>`${s.name} (${s.ticker})`).slice(0,5);return `<div class="specific"><strong>Impatto sui nostri strumenti</strong><p><b>Trading:</b> ${esc(trading||'sensibilità da valutare alla pubblicazione del dato')}.</p><p><b>Investment:</b> ${esc(high.length?high.join(', '):'nessun titolo con sensibilità specifica evidente')}.</p></div>`}
async function load(){try{const r=await fetch(`../data/economic-calendar.json?ts=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw Error(`HTTP ${r.status}`);const d=await r.json();$('#freshness').textContent=`Aggiornato ${when(d.generatedAt)}`;const rows=d.events||[];$('#events').innerHTML=rows.length?rows.map(e=>`<article><div class="top"><time>${when(e.date)}</time><span>${esc(e.country)}</span><span class="impact">${esc(e.importance||'impatto da valutare')}</span></div><h2>${esc(e.title)}</h2><div class="numbers"><span>Precedente <b>${esc(e.previous)}</b></span><span>Atteso <b>${esc(e.forecast)}</b></span><span>Effettivo <b>${esc(e.actual)}</b></span></div><h3>Cosa può succedere</h3><p>${esc(e.generalImpact)}</p>${specific(e,d)}</article>`).join(''):'<article><h2>Nessun evento disponibile</h2><p>La fonte gratuita non ha restituito eventi nel prossimo intervallo. Il calendario verrà riprovato automaticamente al prossimo aggiornamento.</p></article>'}catch(e){$('#events').innerHTML=`<article>Errore caricamento: ${esc(e.message)}</article>`}}load();setInterval(load,300000);